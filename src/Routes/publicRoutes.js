import HomePage from "../Views/HomePage/HomePage";
import { rootPath } from "./path";
const publicRoutes = [
    {
        path: rootPath,
        component: HomePage
    }
]

export default publicRoutes